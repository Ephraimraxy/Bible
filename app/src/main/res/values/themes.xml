<resources>
    <!-- Base application theme. STRICTLY LIGHT MODE -->
    <style name="Theme.OfflineBible" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Brand Colors -->
        <item name="colorPrimary">@color/royal_blue</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/divine_gold</item>
        <item name="android:windowBackground">@color/reading_bg</item>
        
        <!-- Text Colors -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>
</resources>
